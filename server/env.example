# Environment Configuration
# Copy this file to .env and update with your values
# DO NOT commit .env to git!

# Server Configuration
NODE_ENV=development
PORT=1234

# Database Configuration - PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=manufacturing_db
DB_USER=admin
DB_PASSWORD=Admin123
DB_POOL_MAX=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=2000

# CORS Configuration (comma-separated origins, or * for all)
CORS_ORIGIN=*

# Logging
LOG_LEVEL=info

# Application Settings
APP_NAME=Manufacturing Process Production Authenticity
APP_VERSION=1.0.0

# Production Settings (for VPS)
# Uncomment and set for production:
# NODE_ENV=production
# PORT=1234
# DB_HOST=localhost
# DB_NAME=manufacturing_db_production
# CORS_ORIGIN=https://mpr.moof-set.web.id

# API Key for External API Authentication (REQUIRED for Production)
# Generate secure key: openssl rand -hex 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# API_KEY=your_very_secure_random_api_key_change_this_in_production

